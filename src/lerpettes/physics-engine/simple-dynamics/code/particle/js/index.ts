import { createSimpleDynamicsRuntime } from '../../shared/createSimpleDynamicsRuntime';

export default createSimpleDynamicsRuntime({
  runtimeLabel: 'Runtime / particle',
  caption: 'A sphere drifting upward at constant velocity.'
});
